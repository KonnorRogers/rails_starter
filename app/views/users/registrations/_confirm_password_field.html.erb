<%= render FormGroupComponent.new(error: error_for(resource, :password_confirmation)) do %>
  <%= sl_password autocomplete: "new-password", **form_fields(resource, :password_confirmation) %>
<% end %>
